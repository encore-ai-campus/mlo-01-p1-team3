# Requirements change log

> starter_id: `encore.chapter1.brd-prd-requirements-workshop.starter`  
> starter_version: `v1`

baseline 전 문구 수정은 해당 문서 review note에 남긴다. baseline 뒤 요구 의미 변경만 이 파일에 기록한다.

변경이 없으면 다음 한 줄을 유지한다.

```text
No change after v1 baseline
```

변경이 생겼을 때만 아래 표에 행을 추가한다. `REQ-M04`를 선택하지 않았다면 빈 표여도 Must 완료다.

| CR ID | requested_at | reason | affected BRD/PRD/AC | source/schema/schedule impact | migration needed | decision | owner | version/PR/evidence |
|---|---|---|---|---|---|---|---|---|

행을 추가할 때 decision은 `approved | rejected | deferred` 중 하나를 사용한다.
